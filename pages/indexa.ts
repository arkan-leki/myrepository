import { FaArrowAltCircleLeft, FaCartArrowDown, FaCartPlus, FaFilter, FaHome, FaMoneyBill, FaPiggyBank, FaSearch } from 'react-icons/fa';
import { CurrencyDollarIcon, MenuIcon, ReceiptRefundIcon, ShoppingCartIcon, InboxInIcon } from '@heroicons/react/solid'
import { useState } from 'react';

const dashboard = (props) => {
   
    return (
        <div>
            <Head>
                <title>Hawa Phone 1.2 < /title>
                <link rel = "icon" href = "/favicon.ico" />
            </Head>

            < main >
                <Header />
                < Nav />
            </main>

        < /div>
    )
}

export default dashboard